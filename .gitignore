node_modules/
dist/
*.local
.DS_Store

# Local environment. .env.example is the committed template.
.env
.env.*
!.env.example

# Personal Claude Code settings — holds the OpenRouter key. settings.json
# (without .local) is the team-shared one and must stay free of secrets.
.claude/settings.local.json
